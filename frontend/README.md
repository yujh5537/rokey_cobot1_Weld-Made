# frontend (담당: 의석)

React + Three.js. 4버튼(시작·중지·안전복귀·재시작), 단계 표시, 3D(작업대·팁 궤적·접촉점·사각형·외곽 엣지·경로 후보·직육면체), 시간순 로그.
화면 문구는 "외곽 엣지·경로 후보"를 쓴다. CI는 `package.json`이 있으면 `npm ci && npm run build`를 돌린다(`package-lock.json`을 커밋할 것).
