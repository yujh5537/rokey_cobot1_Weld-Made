# 단위·좌표 기준

상태: **v0.1.3** (2026-09-19). 단위 · 기준 규칙은 확정이다. 좌표 **값**은 툴 무게 · TCP · 팁 반지름(T02)과 z=0 · 작업대 원점 · 홈(T03)을 실측했다. 작업대 원점과 탐색 기준점 높이는 초안이고, 축 평행은 판정하지 못했다. BRD 6장에 따라 물품 도착·환경 세팅 후 학민이 확정한다(T02, T03). TBD 값은 코드에 상수로 넣지 않는다. 실측값도 코드에 박지 않고 `contact_scan_bringup/config/real.yaml` 파라미터로 둔다.

## 단위 (확정)
| 구간 | 길이 | 각도 · 자세 | 힘 · 토크 | 속도 |
|---|---|---|---|---|
| 두산 API (posx, get_tool_force) | mm | deg (ZYZ 오일러) | N · N·m | mm/s |
| ROS 내부 (자체 인터페이스) | **m** | **rad · 자세는 quaternion** | **N · N·m** | m/s |
| MQTT·웹 | **mm** | **각도 값을 싣지 않음. 자세는 quaternion** (화면에 deg가 필요하면 웹이 변환) | N · N·m | mm/s |

변환 위치: 두산↔ROS는 **robot_manager**, ROS↔웹은 **mqtt_bridge**. 그 외에서는 변환하지 않는다.

## 시각 (확정)
- ROS: `builtin_interfaces/Time`, 메인 PC ROS 시계. 취득 시각 = robot_manager가 조회 응답을 받은 시각. TCP와 힘의 취득 시각을 따로 보존한다.
- MQTT: epoch ms 정수(UTC). 상세는 `mqtt-schema.md`.
- 웹 PC ↔ 메인 PC 시계 동기는 chrony로 맞춘다(의석, 별도 PR). 관제 지연 측정(TR-05)의 선행조건이다.

## 프레임 (확정)
| 데이터 | 프레임 |
|---|---|
| `RobotSample` · `ContactEvent` · `ExecuteMotion` · `SafetyStatus.position` | **Base** (`base_link`, 가칭) |
| `ScanResult` | **작업대 좌표** (`workpiece_fixture`, 가칭) |

- 샘플 · 이벤트 · 모션은 T03 이후에도 **계속 Base로 발행**한다. 작업대 좌표로의 변환은 **scan_manager 한 곳**에서 결과에만 적용한다(파라미터 `base_to_fixture`).
- **조건: 배치 가이드를 Base 축과 평행하게 설치한다. `base_to_fixture`는 평행 이동만 다룬다.** 밀기 방향(`DIR_POS_X` 등)은 Base 축 기준이다. T03에서는 평행 여부를 판정하지 못했다(아래 좌표 값 표). MVP는 평행 가정으로 진행하고, 회전이 필요해지면 계약을 고친다(`ExecuteMotion.frame_id`로 확장).
- BRD 4.7의 "프레임·단위 변환은 robot_manager에서 관리"는 **두산 좌표(mm·deg) → `base_link`(m·quaternion) 변환**을 가리키는 것으로 해석한다. 결과를 작업대 좌표로 나타내는 것은 형상 계산의 일부다.
- 좌표를 쓰는 쪽(safety_monitor 작업영역, sim 가상 직육면체 등)은 **`frame_id`를 확인하고** 쓴다.
- 프레임 이름은 가칭이다.

## TCP · 힘 기준 (확정)
| 항목 | 기준 |
|---|---|
| TCP 기준점 | **탐침 팁의 최하단점.** 접촉 시 TCP z가 곧 윗면 높이다(반지름을 빼지 않는다) |
| 탐색 중 자세 | **수직 고정.** 그래야 최하단점과 구 중심의 x·y가 같아 모서리 편향 보정식 √(2rδ−δ²)이 그대로 성립한다 |
| 힘 기준 좌표 | `get_tool_force(ref=DR_BASE)`. `RobotSample.wrench`는 `frame_id`(Base) 기준 성분이다 |
| 눌렀을 때 Fz의 부호 | **TBD.** T08에서 손으로 눌러 확인하고 여기와 `docs/env/api-check-log.md`에 적는다 |

## 좌표 값 (일부 실측)
| 항목 | 값 | 확정일 / 확인자 |
|---|---|---|
| 작업대 원점의 Base 좌표 (`base_to_fixture`) | **초안: (423.56, -186.06, 100.6) mm** = (0.42356, -0.18606, 0.1006) m. 정의: 기준 부재(80 mm 큐브)를 놓는 자리의 **윗면 가운데 x · y**(윗면 네 모서리를 찍은 점의 평균), 높이는 z=0(작업대 표면). **2026-09-19 세션 한정 초안이다.** 배치 가이드가 없어 부재를 같은 자리에 놓는 방법(표시 · 스토퍼)은 정하지 않았다. **가이드(직각 스토퍼)를 설치하면 가이드 모서리 기준으로 다시 정의한다.** z 100.6은 TCP z 249.99에 종속된다(TCP z를 고치면 같이 고친다). 근거: `docs/env/origin-home-register.md` | 2026-09-19 / 학민 · 확인: PR #60 리뷰 (현지 · 병후) |
| 배치 가이드와 Base 축의 평행 확인 | **판정 불가.** 가이드가 없어 기준 큐브 윗면 네 모서리를 찍어 변 방향을 쟀다(Base 축과 0.1~1.5°, 참고값). 그러나 네 모서리가 모두 2~3 mm 안쪽으로 찍혀(아래 "기준 큐브 모서리 상태") 이 각도로는 정렬을 판정할 수 없다. 영향은 둘로 나뉜다. 1~2° 회전은 **폭 · 길이 치수에는 0.05 mm 이하**로 들어와 MVP 지표에 영향이 없다. **꼭짓점 · 경로 후보 좌표에는 축당 0.7~1.4 mm**가 남는다(80 mm 부재, 중심에서 40 mm). 가이드를 설치하면 다시 확인한다 | 2026-09-19 / 학민 · 확인: PR #60 리뷰 (현지 · 병후) |
| z=0 기준 (작업대 표면인가 지그 지지면인가) | **작업대 표면** (지그 없이 부재를 작업대에 바로 놓는다). Base z = **100.6 mm**. 194 mm 떨어진 두 점이 100.49 · 100.71이었다. 검산: 큐브 윗면 10점 평균 180.69 − 100.6 = 80.1 mm이고, 캘리퍼로 잰 8 cm와 맞는다. 다만 **80.1 mm는 평균끼리 뺀 값**이다. 윗면 10점의 산포는 **179.3~181.5 mm**이고(모서리 4점이 안쪽 점보다 약 0.9 mm 낮다), 점 하나의 높이 정밀도로 읽지 않는다 | 2026-09-19 / 학민 · 확인: PR #60 리뷰 (현지 · 병후) |
| 탐침 TCP 오프셋 (최하단점 기준) | **[-1.30, 3.71, 249.99] mm, 자세 [0, 0, 0] deg (가정)** (플랜지 기준, 불확도 약 ±1 mm). 아래 구 중심 z에 r을 더했다. 컨트롤러 이름 `rg2_probe_tip` | 2026-09-19 / 학민 · 확인: PR #57 리뷰 (현지) · `docs/env/tool-tcp-register.md` 3절 |
| (실측) 탐침 팁 구 중심의 오프셋 (플랜지 기준, x · y · z, 자세) | [-1.30, 3.71, 249.76] mm, 자세 [0, 0, 0] deg (가정). 피벗 보정 14자세, 불확도 약 ±1 mm. 피벗 보정은 팁을 홈에 앉혀 했으므로 구한 점은 구 중심이다 | 2026-09-19 / 학민 · 확인: PR #57 리뷰 (현지) · `docs/env/tool-tcp-register.md` 3절 |
| 툴 무게·무게중심 (RG2 + 탐침) | 1.3 kg, 무게중심 (0, 31.08, 29.84) mm (플랜지 기준). 펜던트에 있던 값이고 탐침 0.3 g은 무시했다. 무게중심 y 치우침의 출처는 미확인. 컨트롤러 이름 `rg2_probe` | 2026-09-19 / 학민 · 확인: PR #57 리뷰 (현지) · `docs/env/tool-tcp-register.md` 2절 |
| 홈위치(시작위치) 관절각 | **[-24.14, 17.03, 51.68, -0.18, 111.39, -204.84] deg** (J1~J6). 원점 바로 위, 탐침 수직. 참고: 이 자세에서 읽은 posx는 (424.46, -183.09, 539.96, 20.76, -179.87, -159.97)이다. 그때 TCP가 풀려 있었을 가능성이 높아 플랜지 좌표로 보며, 팁 높이는 약 z 290.0(작업대 위 189.4 mm)이다. TCP 상태는 확인하지 않았다. **안전복귀(`OP_HOME`) 경로는 TBD**다(아래 미결정). **주의: J6 −204.84°는 ±180° 밖이다.** 다른 자세에서 이 값으로 movej하면 J6가 크게 돌 수 있다 | 2026-09-19 / 학민 · 확인: PR #60 리뷰 (현지 · 병후) |
| 탐색 기준점(원점 상공) 높이 | **초안: 첫 하강은 홈 자세의 팁 높이(작업대 위 189.4 mm)에서 시작한다.** 첫 하강 전에는 부재 윗면 높이를 모르므로 "윗면 위 N mm"로 정할 수 없다. 기준점 높이는 홈과 `base_to_fixture`로 정해지므로 별도 파라미터를 두지 않는다. 80 mm 기준 부재면 윗면 위 109.4 mm다. 방향 전환 때의 상공 높이와 재접근은 `ros-interfaces.md` 7.3(`lift_height_m` · `recontact_margin_m`)을 따른다. **`max_descend_m` 조건:** 하한 = 홈 팁 높이 − 부재 높이 + 여유 5 mm(80 mm 부재면 **0.115 m**). 상한 < 홈 팁 높이 − 안전 여유(0.189 m − 여유). 상한을 넘으면 부재가 없을 때 팁이 작업대에 닿아 작업대 면을 윗면으로 잡는다. 두 조건을 같이 만족하지 못하는 낮은 부재는 홈을 낮춘다. 여유 값은 하강 속도 · 감속 거리로 T13에서 확정한다 | 2026-09-19 / 학민 (초안) · 확인: PR #60 리뷰 (현지 · 병후) |
| 팁 반지름 r | **0.225 mm** (인공눈물 용기 끝, 지름 0.45 mm 실측. 설계 출발값 3 mm를 대체) | 2026-09-19 / 학민 · 확인: PR #57 리뷰 (현지) |

**기준 큐브 모서리 상태 (미확인 가설, 2026-09-19).** 윗면 네 모서리가 모두 기대 위치보다 안쪽으로 찍혔다. 중심에서 모서리까지 53.5~54.5 mm로, 기대값 56.57 mm보다 평균 2.39 mm 짧다. 서로 간 편차는 1.0 mm뿐이다. 모서리 z 평균(180.14)도 안쪽 4점(180.50~181.51)보다 약 0.9 mm 낮다. 손으로 찍은 잡음이라면 한 방향으로 가지런히 모이지 않고 z가 낮아질 이유도 없으므로, **큐브 윗면 모서리에 라운드나 모따기가 있을 가능성이 크다.** 라운드라면 변당 폭이 최대 약 1.7 mm이고, 윤곽 탐색에서 양쪽 변이 모두 안쪽에서 걸려 **폭 · 길이가 최대 약 3.4 mm 과소 추정될 수 있다.** 이 두 값은 꼭짓점에서 유도한 **상한**이다(꼭짓점은 두 변의 라운드가 만나는 자리라 변 가운데보다 크게 나온다). 실제 값은 아래 2번 시험으로 정한다. 다음 실기 세션에서 확인한다(근거와 계산: `docs/env/origin-home-register.md`).
1. 모서리가 둥근지 캘리퍼 · 손끝으로 확인하고 라운드 폭을 잰다
2. 변 하나를 따라 안쪽에서 바깥으로 0.5 mm 간격으로 찍어 z가 떨어지기 시작하는 거리를 기록한다(큐브는 옮기지 않는다)
3. 윗면 안쪽만 여러 점 찍어, 큐브가 기운 것인지 모서리에서 낮게 읽힌 것인지 구분한다

**툴 · TCP 등록은 휘발성이다 (2026-09-19 실기 확인).** ROS 서비스로 등록한 툴 · TCP는 `sodreal`을 다시 켜면 지워진다. 그러면 컨트롤러는 펜던트 설정(툴 없음, TCP `GripperDA_v1` 등)으로 돌아간다. 툴이 없으면 무접촉 정지 상태에서도 외력이 11~12.5 N(아래 방향)으로 읽혀 접촉 판정이 불가능하다. 등록한 상태에서는 2.1~2.9 N(순간 최대 3.7 N)이었다. 그래서 robot_manager는 시작할 때와 탐색 전에 현재 툴 · TCP를 확인해야 한다(BRD 4.1.5). 펜던트의 공용 TCP `GripperDA_v1`은 고치지 않는다.

홈위치(로봇 시작위치)와 작업대 원점은 다른 것이다. 방향별 탐색 사이의 "원점 복귀"(`OP_MOVE_TO`)와 "홈 복귀"(`OP_HOME`: 관제자의 안전복귀, 스캔 정상 완료 뒤의 마무리)도 다른 동작이다.

## 미결정 (BRD 6장, 팀 확인 없이 정하지 않는다)
홈 복귀 경로·순서, 중단 위치 재접근 절차, 이상 상태별 재시작 허용 조건, 정지 우선순위, 데이터 최신성 한계, heartbeat 만료 정책.
(래치 해제 조건은 확정됐다: 관제자의 `/safety/reset` + 감시 조건 해소.)
