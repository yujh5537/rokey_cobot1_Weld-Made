# 2026-09-23 실기 측정 — phase 2 용접 준비 (연휴 전 마지막 실기)

**담당: 정 학민 · 부 병후.** 9/24~9/28 은 로봇을 못 쓴다. 아래 M1~M6 이 오늘 없으면 연휴 중 짠 코드(P1 robot_manager · P2 weld_manager)가 9/29 에 안 맞을 수 있다.
**우선순위 M1 > M3 > M2 > M4 > M5 > M6.** 결과는 이 파일의 표를 채우고 CSV · 사진을 `docs/test-reports/data/20260923_weld/` 에 넣어 **이 PR 에 커밋**한다. 실기 명령은 사람이 한다(CLAUDE.md 규칙 1).

배경: phase 2 계약(PR #184 `docs/phase2/`)은 툴을 두 면 법선의 이등분면 안에서 연직 45° 기울이고, 이음선에서 3 mm 띄워 8 개 모서리를 따라 간다. **이 자세를 로봇이 실제로 만들 수 있는지(M1), 손가락 · 홀더가 큐브 · 작업대에 닿지 않는지(M2), 경유점 이동이 어떤 서비스로 되는지(M4)** 를 오늘 봐야 한다.

전제: TCP `[0, 0, 252.12]`(`rg2_probe_tip`) · 툴 `rg2_probe` 등록 세션(`docs/env/apply_tool_tcp.py` 마지막 줄 `OK`). 큐브 좌표는 origin/main `real.yaml` · `units-frames.md` v0.1.18 값(Base mm): x⁻ 378.48 · x⁺ 462.03 · y⁻ −198.75 · y⁺ −114.60 · 윗면 z 178.003 · 작업대 95.006 + 테이프 2 = 97.0. 큐브를 옮겼으면 스텝 모드 스캔으로 네 모서리를 다시 재고 `--cube` 로 넣는다.

## 안전
- 입회 1 명이 비상정지 손잡이를 잡는다. 모든 이동은 **10 mm/s**, 먼저 자세 **위 30 mm** 로 간 뒤 내려간다(스크립트가 그 순서다).
- 45° 자세는 1차에서 한 번도 안 해 본 자세다. 첫 자세(L0 start)는 Virtual 에서 먼저 돌려 오류가 없는지 본다.
- 실기에서 `success=false`(관절 한계 · 특이점)가 나오면 그 자세는 넘어간다. 억지로 조그로 만들지 않는다 — 대신 아래 "막힐 때" 절차.
- 과대 외력 정지 · 충돌 뒤에는 `units-frames.md` "이벤트 점검"(기준점 재접촉)을 한다.

## M1. 기울인 자세 도달성 (학민, 입회 병후) — 가장 중요

16 자세 = 8 선의 시작 · 끝(세로선은 위 · 아래). 계산: `docs/env/weld_pose_check.py`(`weld-motion.md` 2 · 3절 식. 홈 quaternion 검산 통과, ZYZ 왕복 오차 1e-16).

```bash
sod && cd ~/ws_cobot_pjt
python3 docs/env/weld_pose_check.py --dry-run                      # 표와 같은 16 줄이 나오는지
# ① Virtual 먼저 (다른 터미널에서 sodvir)
python3 docs/env/weld_pose_check.py --vel 30 --file /tmp/vir.csv
# ② 실기 (sodreal, 툴 · TCP 등록 뒤)
python3 docs/env/weld_pose_check.py --real-ok --vel 10 --file docs/test-reports/data/20260923_weld/m1_poses.csv
```
스크립트는 자세마다 위 30 mm → Enter → 자세 → posx · posj 읽어 CSV → 간섭 거리(자로 잰 mm) · 메모 입력 → Enter → 위로. `s` 건너뜀, `q` 종료. `--only L0,L4` 로 일부만.

| 선 | 위치 | posx [x, y, z, A, B, C] (mm · deg) | 도달 (O/X · 오류) | posj (deg) | 간섭 최소 거리 (mm) | 메모 |
|---|---|---|---|---|---|---|
| L0 | start | [378.48, −200.87, 180.12, 90.00, 135.00, −180.00] | O | [-51.6, 34.6, 60.0, 30.4, 119.7, 45.6] | 5.0 |  |
| L0 | end | [462.03, −200.87, 180.12, 90.00, 135.00, −180.00] | O | [-45.8, 44.5, 43.0, 35.6, 122.5, 55.7] | 5.0 |  |
| L1 | start | [464.15, −198.75, 180.12, −180.00, 135.00, −180.00] | **X** | — | — | **도달 불가.** 플랜지 761 mm. 45° · 30° · movejx sol 0~3 · 롤 180° 전부 안 움직임 |
| L1 | end | [464.15, −114.60, 180.12, −180.00, 135.00, −180.00] | **X** | — | — | **도달 불가.** 위와 같다 |
| L2 | start | [462.03, −112.48, 180.12, −90.00, 135.00, −180.00] | O | [16.9, 17.9, 85.0, -42.6, 92.6, 291.1] | 1.5 |  |
| L2 | end | [378.48, −112.48, 180.12, −90.00, 135.00, −180.00] | O | [20.4, 7.2, 98.3, -41.6, 92.8, 295.3] | 2.0 |  |
| L3 | start | [376.36, −114.60, 180.12, 0.00, 135.00, −180.00] | O | [-49.0, -29.4, 127.0, 39.9, 56.3, 116.1] | 3.0 |  |
| L3 | end | [376.36, −198.75, 180.12, 0.00, 135.00, −180.00] | O | [-62.4, -18.5, 121.1, 45.4, 61.6, 100.9] | 5.0 |  |
| L4 | top | [376.98, −200.25, 180.12, 45.00, 135.00, −90.00] | O | [-65.1, 10.7, 93.8, 41.7, 93.2, -204.3] | (1.1) 미확정 | 측정 방식 의심 — 보고서 2절 |
| L4 | bottom | [376.98, −200.25, 104.13, 45.00, 135.00, −90.00] | O | [-65.1, 13.5, 100.4, 41.7, 86.2, -210.5] | (0.8) 미확정 | 측정 방식 의심 — 보고서 2절 |
| L5 | top | [463.53, −200.25, 180.12, 135.00, 135.00, −90.00] | **X** | — | — | **도달 불가.** 플랜지 762 mm |
| L5 | bottom | [463.53, −200.25, 104.13, 135.00, 135.00, −90.00] | **X** | — | — | **도달 불가.** 위와 같다 |
| L6 | top | [463.53, −113.10, 180.12, −135.00, 135.00, −90.00] | O | [5.7, 44.5, 42.8, -33.5, 125.5, 39.0] | (0.5 · 0.4) 미확정 | 2 회 측정. 측정 방식 의심 |
| L6 | bottom | [463.53, −113.10, 104.13, −135.00, 135.00, −90.00] | O | [5.7, 44.8, 51.6, -30.5, 117.7, 44.7] | (0.4) 미확정 | 측정 방식 의심 |
| L7 | top | [376.98, −113.10, 180.12, −45.00, 135.00, −90.00] | O | [18.8, -22.9, 123.4, -44.8, 64.2, -11.4] | (0.5) 미확정 | 측정 방식 의심 |
| L7 | bottom | [376.98, −113.10, 104.13, −45.00, 135.00, −90.00] | O | [18.8, -20.0, 131.6, -49.4, 56.6, -2.0] | (9.0) 미확정 | 혼자 반대로 튄다 |

- A · B · C = 두산 posx 의 ZYZ 오일러(deg). B = 135 가 "연직에서 45° 기울임"이다(수직 자세는 B = 180).
- 세로선 bottom(z 104.13)은 작업대 + 테이프 2 + 여유 5 + 스탠드오프 수직 성분 2.12 다. **손가락이 작업대에 닿을 위험이 가장 큰 자세**다. 위 30 mm 에서 눈으로 보고 내려간다.
- **막힐 때**: ① 그 자세에서 툴 축 둘레로만 J6 를 ±30° · ±60° 돌려 되는 각을 찾고 posj 를 적는다(→ 파라미터 `tool_roll_deg` 후보). ② 전부 막히면 `--tilt 30` 으로 다시 돈다(`tilt_deg` 는 파라미터다). ③ 그래도 안 되면 그 선을 시연에서 빼는 것도 답이다 — 어느 선인지만 적는다.
- **판정 기준**: 16 자세 중 되는 자세 수와 막힌 자세의 원인(관절 한계 / 특이점 / 간섭). 간섭 최소 거리 < 10 mm 면 `standoff_m` 을 올리거나 `approach_m` 경로를 바꾼다(P2 에 전달).

### 결과 (2026-09-23 21:02~21:57 실기, 학민 · 입회 병후)

**12 / 16 도달. L1 start · end 와 L5 top · bottom 불가.**
정리: `docs/test-reports/m1-measurements_20260923.md` · 경과와 원인 분석: `docs/test-reports/m1-weld-pose-check_20260923.md`

- 막힌 원인은 **도달거리**다. 플랜지 ↔ 베이스 거리순으로 L3 426 · L7 437 · L4 546 · L2 587 · L0 644 · L6 689 ✅ / L1 761 · L5 762 ❌ → **45° 자세의 도달 한계는 689~761 mm 사이**. 45° · 30° · `movejx` sol 0~3 · 손목 롤 180° 를 다 시도해 전부 실패했고, 수직 0° 도 665 mm 로 경계다. 큐브 재배치 · 90° 회전도 답이 아니다(보고서 1절)
- 도달한 13 측정(L6 top 2 회)의 **위치 오차는 최대 0.14 mm**(지령 대비 유클리드). 육안 접촉 없음
- ⚠️ **간섭 거리 열은 윗면(L0 · L2 · L3)만 확정이다.** 실측 평균 3.58 vs 설계 3.00 으로 스탠드오프 3 mm 는 검증됐다. 세로선(스탠드오프 6)은 0.4~1.1 로 설계 6.00 과 안 맞는데, 같은 꼭짓점에서 L0 start(설계 3.00 → 5.0)와 L4 top(설계 6.00 → 1.1)이 **역방향**이라 기하로 설명되지 않는다. TCP x·y · 큐브 좌표 · 탐침 길이를 다 배제했다 → **측정 방식 문제로 보고 캘리퍼 재측정 전까지 미확정**(괄호 값은 참고용)
- ⚠️ **bottom 네 자세는 계약 z 104.13 이 아니라 121.25 에서 돌았다**(+17.12 mm, "아래 여유 20"). 계약 z 에서의 도달성 · 작업대 간섭은 **미실시**다. 9/29 세로선 시연 전에 이 네 자세를 계약 z 로 먼저 확인해야 한다
- 스크립트 결함 2 건을 이 브랜치에서 고쳤다: `reach` 가 `move_line` 의 `success` 만 보던 것(→ 지령 대비 1 mm 비교), 자세 사이 안전 높이가 없어 **큐브와 실제로 충돌**한 것(→ 수직 상승 → 안전 높이 이동 → 수직 하강 3 단계, `--safe-z`)

## M2. 툴 치수 (학민, 캘리퍼 · 자)
45° 로 기울이면 탐침 자루 · 홀더 · RG2 손가락이 이음선 위 · 바깥으로 뻗는다. 세로선 bottom 에서 손가락이 작업대와 얼마나 떨어지는지는 **노출 길이** 가 정한다: 손가락 끝의 높이 ≈ 팁 z + 노출 길이 × cos45°. 노출 길이 60 mm 면 팁 위 42 mm, 작업대 위 약 49 mm(여유 있음). 30 mm 면 21 mm(간섭 위험).

| 항목 | 값 (mm) | 비고 |
|---|---|---|
| RG2 손가락 끝 → 탐침 팁 노출 길이 | | |
| 탐침 홀더 최대 지름 | | |
| 손가락 끝 폭(닫힌 상태) · 손가락 두께 | | |
| 손가락 끝 → 플랜지 거리 | | = 252.12 − 노출 길이 (검산) |
| 사진 | | 45° 자세에서 옆에서 1 장, 위에서 1 장 |

## M3. 오늘 실기 스캔 `result.json` 복사 (그 PC 를 쓴 사람 → 현지)
- 위치: 실기 bringup 을 띄운 셸의 현재 디렉터리 기준 `ws_cobot1/data/<scan_id>/result.json`(`result_dir: data`). 성공 3 회의 `scan_id` 는 `/scan/result` 로그 또는 `data/` 최신 디렉터리.
- 레포 `docs/phase2/fixtures/real_<scan_id>.result.json` 으로 복사(+ 같은 디렉터리의 `progress.json` 은 선택). **연휴 중 weld_manager · 웹 개발의 실제 입력이다.** `node_params.base_to_fixture` 가 들어 있는지 확인.
- 세 결과의 `vertices` 가 서로 3 mm 안에서 같은지 한 줄로 적는다(D18 허용오차의 근거).

| scan_id | success | width · length · height (mm) | 파일 |
|---|---|---|---|
| `20260923-183211-3702` (18:32) | `true` | 83.00 · 80.64 · 80.76 | `docs/phase2/fixtures/real_20260923-183211-3702.result.json` (+ `.progress.json`) |
| 17:34 | — | — | **unstable 로 죽음** |
| 17:43 | — | — | **unstable 로 죽음** |

⚠️ **성공 회차가 1 회뿐이다.** 표의 "3 회" 전제와 `vertices` 를 3 회 비교해 D18(±3 mm) 근거를 만드는 것은 **할 수 없다.** 연휴 중 weld_manager · 웹 개발 입력으로는 이 한 건을 쓴다. `node_params.base_to_fixture` 는 들어 있다(`[0.420255, -0.156675, 0.095006]`).

## M4. 경유점 이동 방식 (학민)
`ExecutePath`(P1)의 `path_mode` 두 가지(`line` = 점마다 amovel 정지, `spline` = `move_spline_task` 한 번)를 실제 호출로 확인한다. **"`move_line` + radius 블렌딩"은 시험하지 않는다**: 드라이버가 ASYNC 에서 radius 를 버린다(`dsr_controller2.cpp` 464행, 현지 소스 확인 2026-09-24). **소스 확인에 그치지 말고 호출 확인까지 한다**(병후): 아래 표를 Virtual 과 실기 각각 채운다. **큐브 위 100 mm 공중**에서 지그재그 11 점(진행 4 mm 간격 · 좌우 ±2 mm), 10 mm/s. 자세는 수직(홈 자세) 그대로.

| 방식 | 서비스 success · 응답 지연 | 점 사이에서 멈추나 | `get_robot_state` MOVING 유지 | `move_stop` 즉시 정지 | 마지막 점 오차 (mm) | 판정 |
|---|---|---|---|---|---|---|
| `move_spline_task` ASYNC (`pos` = Float64MultiArray ×11, `pos_cnt` 11, `opt` 0 → 1 도 시도) | | | | | | |
| `move_spline_task` ASYNC, 100 점 · 101 점 (배열 한도 `MAX_SPLINE_POINT` 100, 컨트롤러가 `pos_cnt` 검사 안 함 — 101 은 **Virtual 에서만**, 실기 금지) | | | | | | |
| `move_line` ×11 SYNC, radius 0 (정지 허용) | | | | | | |

- Virtual 에서 먼저(에뮬레이터가 `move_spline_task` 를 받는지). 실기에서 한 번은 본다 — 에뮬레이터와 다를 수 있다.
- 결과를 `docs/env/api-check-log.md` 에 [E-번호] 로 적는다(성공 · 지연 · 비고).
- 하나라도 "점 사이에서 안 멈추고, move_stop 이 듣고, 오차 ≤ 3 mm" 면 그것으로 P1 을 간다. 전부 멈추면 정지 허용(D8)으로 간다.

### 실기 미실시 — Virtual 결과로 결론이 먼저 났다 (2026-09-24)

9/23 밤 실기는 M1 만 돌렸다. **이 표의 실기 칸은 전부 미실시다.**

그 사이 P1(#191 `965efed`)이 Virtual 로 같은 것을 확인했고 결론이 났다:

| 방식 | Virtual 결과 (#191, 현지) | 판정 |
|---|---|---|
| `move_spline_task` ASYNC | 응답이 점 수에 비례해 늦다 — 10 점 0.31 s · 21 점 0.5~0.67 s · 100 점 2.4~3.2 s. 그동안 `CallQueue` 가 막혀 샘플이 끊긴다 → `SAMPLE_STALE`. 21 점 이상은 큐 한도(0.5 s)에 걸려 "명령 실패"로 끝나는데 컨트롤러는 실행 중이었다 | **쓰지 않는다** |
| `move_line` 반복 (amovel, ASYNC) | 21 점 `TARGET_REACHED` · 18.2 s · 점 통과 오차 **0.0 mm** · 최대 샘플 공백 69 ms. 실행 중 `/robot/stop` 0.13 s 에 정지 | **채택 — `path_mode: line`(D31)** |
| `move_line` + radius 블렌딩 | 시험하지 않음. 드라이버가 ASYNC 에서 radius 를 버린다(`dsr_controller2.cpp` 464 행) | 선택지 아님 |

→ **P1 은 M4 실기를 기다리지 않는다.** 실기 1 회 확인(에뮬레이터와 다를 수 있으므로)과 `docs/env/api-check-log.md` 기록은 **연휴 후 학민**이 채운다. 101 점 항목은 Virtual 전용(실기 금지)인 것을 확인했다.

## M5. 큐브 고정 상태 · 치수 (현지)
| 항목 | 값 |
|---|---|
| 본드 고정 여부 (9/29 까지 유지되나) | |
| 캘리퍼: 가로 · 세로 · 높이 (mm) | |
| 테이프 두께 (mm) | |
- 유지되면 오늘 스캔 결과(M3)로 9/29 에 바로 용접 리허설을 할 수 있다. 옮기면 9/29 에 스캔부터(통합 순서 1).

## M6. 속도 느낌 (학민, 선택)
공중에서 `movel` 50 mm/s(선 사이 이동 출발값) · 10 mm/s(용접 출발값)로 200 mm 왕복. 흔들림 · 소음 · 정지 오버슈트. 시연에서 보기 좋은 속도.

| 속도 | 느낌 · 오버슈트 (mm) | 시연 후보 |
|---|---|---|
| 10 mm/s | | |
| 30 mm/s | | |
| 50 mm/s | | |

## 결과가 바꾸는 것 (P1 · P2 에 전달)
| 결과 | 바뀌는 곳 |
|---|---|
| M1 막힌 자세 · `tool_roll_deg` · tilt 30 | `weld-motion.md` 2절 · `real.yaml` weld_manager `tilt_deg` · `tool_roll_deg` |
| M2 노출 길이 | `standoff_m` · `approach_m` · `bottom_margin_m` 출발값 |
| M4 방식 | P1 `dsr_client.py` (spline / line), `ExecutePath` 계약 5.2 의 "점 사이 정지" 문구 |
| M3 fixtures | P2 · P3 의 개발 입력 |
