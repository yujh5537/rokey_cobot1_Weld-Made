# 04. 하드웨어 구성

근거: 레포 `docs/contracts/units-frames.md`(v0.1.18) · `docs/env/tool-tcp-register.md` · `docs/env/versions.md` ·
`ws_cobot1/src/contact_scan_bringup/config/real.yaml` · `docs/test-reports/realrobot-session_20260921.md`
작성: 학민 · 2026-09-24

## 1. 장비 목록

| # | 항목 | 사양 · 값 | 출처 |
|---|---|---|---|
| 1 | 협동로봇 | **두산 M0609** (6축, 가반 6 kg, 명목 도달거리 900 mm, 반복정밀도 ±0.03 mm) | BRD [E4] |
| 2 | 로봇 컨트롤러 | DRCF `GF02120100` · DRFL `GL013303`, IP **192.168.1.100:12345** | `versions.md` (실기 로그, 2026-09-21) |
| 3 | 그리퍼 | **OnRobot RG2**. 탐침을 고정 파지만 한다 — **폭 변화는 접촉 판정에 쓰지 않는다** | BRD 결정, ADR 0001 |
| 4 | 탐침 | 인공눈물 용기(빈 용기). 무게 **0.3 g**, 팁 반지름 **약 2 mm**(캘리퍼 확정 전) | `tool-tcp-register.md`, `real.yaml tip_radius_m` |
| 5 | 탐침 홀더 | 3D 프린트. RG2 핑거 형상에 유격 없이 맞물린다. 핑거 끝 → 팁 돌출 **12.5 mm** | `tool-tcp-register.md` 59 행 |
| 6 | 부재(기준 큐브) | 정육면체. 설계 80 mm, `real.yaml` 주석 **약 81 mm**(캘리퍼 확정 전). 9/23 스캔 실측 폭 83.00 · 길이 80.64 · 높이 80.76 mm | `real.yaml`, `docs/phase2/fixtures/real_20260923-183211-3702.result.json` |
| 7 | 고정 | 코끼리테이프 위에 본드 고정. 테이프 두께 **약 2 mm(추정, 미측정)** | `real.yaml support_z_m`, #147 |
| 8 | 작업대 | 2026-09-22 교체(옛 작업대 눌림). 표면 z = Base **95.006 mm** | `units-frames.md` 44 행 |
| 9 | 메인 PC | Ubuntu 24.04 / ROS 2 Jazzy / Python 3.12, RMW `rmw_fastrtps_cpp`, `ROS_DOMAIN_ID` 30 | `versions.md` |
| 10 | 웹 PC | Docker 4 서비스(mosquitto · postgres · FastAPI · Spring Boot) + React/Vite. 버전 **TBD(의석)** | `versions.md`, `02-network.md` |
| 11 | 네트워크 | 로봇 ↔ 메인 PC 유선, 메인 PC ↔ 웹 PC MQTT 1883 | 루트 README Runbook |

> **미실시**: 홀더 최대 지름 · 핑거 끝 폭 · 두께 · 핑거가 툴 축에서 뻗은 반폭은 아직 재지 않았다(#186 M2).
> 부재 캘리퍼 치수와 테이프 두께도 미측정(#186 M5)이다. 연휴 중에는 장비가 없어 **9/29 오전**에 잰다.

## 2. 좌표 기준 (모두 실측. 좌표 계열은 **v0.1.18 잠정**)

| 기준 | 값 | 어떻게 쟀나 |
|---|---|---|
| **TCP** (플랜지 → 탐침 팁) | `[0, 0, 252.12]` mm | z: 작업대 기준점을 힘 접촉(증분 3 N)해 판정 좌표로. x · y: **J6 를 180° 돌렸을 때 팁이 0.03 mm 안에서 제자리** → (0, 0) 확정 (#137) |
| **z = 0** (작업대 표면) | Base **95.006 mm** | 정의점 (524.97, −172.03) 1 점, 힘 접촉 판정 좌표. 줄자 94.4 mm 와 0.6 mm 차 |
| **작업대 원점** `base_to_fixture` | (420.255, −156.675, 95.006) mm | x · y = 9/22 스텝 모드 네 모서리의 가운데(방향마다 같은 편향은 가운데에서 상쇄). **z 는 윗면이 아니라 지지면이다** |
| **홈** `home_joint_deg` | `[-21.19, 15.24, 52.97, -0.08, 111.80, -15.14]` deg | 관절각으로 둔다. 자세(posx)가 아니다 |
| **탐색 기준점** `search_origin_pose` | (420.255, −156.675, 218.0) mm + 홈 자세 | 부재 윗면(178.003 ± 0.056 mm, 10 회) + 40 mm. **40 mm 인 이유**: 하강 판정이 둔한 구간(3 mm/s × 6.5 s ≈ 20 mm)보다 멀어야 한다 |

### 왜 이 순서로 쟀나 (미지수 둘을 하나씩 닫는다)

탐침 교체와 작업대 교체가 겹쳐 **TCP z 와 z=0 을 동시에 잃었다.** 식이 하나뿐이라 순서가 필요했다.

1. 캘리퍼로 새 탐침 길이를 재 **TCP z 를 잠정 등록**
2. 그 상태로 **지지면을 힘 접촉**해 읽힌 z 를 **z=0 으로 확정**
3. **큐브 윗면 접촉 높이를 캘리퍼 80 mm 와 검산** ← 독립적으로 아는 값이 여기서 TCP z 오차를 잡는다
4. **J6 180° 회전으로 TCP x · y 확인** (절대 좌표에 그대로 들어간다)
5. 팁 반지름 재측정 → 6. 툴 무게 · 무게중심 재등록

## 3. 툴 등록값

| 항목 | 값 | 비고 |
|---|---|---|
| 툴 이름 / TCP 이름 | `rg2_probe` / `rg2_probe_tip` | 컨트롤러 등록 |
| 총 무게 | **1.3 kg** | 탐침 0.3 g 은 반올림에서 빠진다 |
| 무게중심 (플랜지 기준) | (0, 31.080, 29.840) mm | 펜던트 값. **RG2 는 좌우 대칭이라 y 31 mm 는 확인 필요** |

> ⚠️ **등록은 세션마다 휘발된다.** `sodreal` 재기동이나 launch 중 `apply_tool_tcp.py` 실행이면 지워진다.
> 빠뜨리면 좌표가 팁이 아니라 **플랜지**로 나오고(+252.12 mm) 외력이 약 **12 N** 치우친다(#123).
> 세션 시작마다 `python3 docs/env/apply_tool_tcp.py --tcp-x 0 --tcp-y 0` → `OK: ... [0.0, 0.0, 252.12]` 를 확인한다.

## 4. 배치 사진

**미실시 — 9/29 오전에 촬영해 넣는다.** 필요한 컷: ① 전체 배치(로봇 · 작업대 · 큐브, 치수 표기)
② 탐침 · 홀더 · RG2 근접 ③ 45° 용접 자세 옆 · 위(#186 M2). 파일은 `docs/presentation/assets/hakmin/` 에 둔다.

## 5. 알려진 한계

- **z = 0 이 1 점이다.** #147 합격 기준은 4 점 이상(300 mm 에 0.5 mm)이고 아직 미실시다.
- **테이프 두께 2 mm 는 추정값이다.** 부재 높이가 그만큼 크게 나온다.
- **45° 용접 자세는 16 자세 중 12 만 도달한다.** L1 · L5 는 플랜지가 761 · 762 mm 로 나가 불가하고,
  기울기 30° · `movejx` sol 0~3 · 손목 롤 180° 로도 해결되지 않는다(#186 M1).
