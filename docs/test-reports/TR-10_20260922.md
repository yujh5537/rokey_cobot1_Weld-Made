# TR-10 저장·조회 연동 시험 — 2026-09-22

## 1. 시험 목적

BRD TR-10의 저장·조회 연동 요구사항을 검증한다.

검증 흐름:

```text
Main PC result_store
        ↓
mqtt_bridge
        ↓
Mosquitto
        ↓
FastAPI
        ↓
PostgreSQL
        ↓
Spring Boot 조회 API
```

확인 항목:

1. Main PC 원본과 PostgreSQL 저장 결과의 식별자 및 측정값 일치
2. PostgreSQL과 Spring Boot 조회 결과 일치
3. ROS 내부 단위(m) → Web/DB 단위(mm) 변환 일치
4. 실제 적용된 ScanConfig 저장·조회 일치
5. 미측정/실패값과 정상적인 `0` 값 구분
6. MQTT 메시지 수신과 DB 저장 완료를 구분하여 기록하는지 확인

## 2. 시험 환경

| 항목 | 값 |
|---|---|
| 시험일 | 2026-09-22 |
| Branch | `euiseok/20260922-t41` |
| Main PC | ROS 2 Jazzy / DSR Virtual / `source:=sim` |
| Web PC | Mosquitto / FastAPI / PostgreSQL / Spring Boot / React |
| ROS_DOMAIN_ID | `35` |
| MQTT Broker | `172.24.0.51:1883` |
| 정상 시험 Scan ID | `20260922-205447-1618` |
| 실패/미측정 확인 Scan ID | `20260922-202652-9248` |

## 3. 정상 Scan 결과

Main PC `result_store`:

```text
data/20260922-205447-1618/
├── progress.json
└── result.json
```

Main PC `result.json` 기준:

```text
success      = True
frame_id     = workpiece_fixture

z_top        = 39.865005 mm
x_pos        = 49.934983 mm
x_neg        = -49.844991 mm
y_pos        = 29.965002 mm
y_neg        = -29.955002 mm

width        = 99.779974 mm
length       = 59.920004 mm
height       = 39.865005 mm

support_z    = 0.000000 mm

dims_valid   = True
box_valid    = True
```

## 4. Main PC ↔ PostgreSQL ↔ Spring Boot 대조

| 항목 | Main PC result_store | PostgreSQL | Spring Boot | 판정 |
|---|---:|---:|---:|---|
| `scan_id` | `20260922-205447-1618` | 동일 | 동일 | PASS |
| `success` | true | true | true | PASS |
| `frame_id` | `workpiece_fixture` | 동일 | 동일 | PASS |
| `z_top` | 39.865005 mm | 39.86500537109375 mm | 동일 | PASS |
| `x_pos` | 49.934983 mm | 49.93498291015625 mm | 동일 | PASS |
| `x_neg` | -49.844991 mm | -49.8449914550781 mm | 동일 | PASS |
| `y_pos` | 29.965002 mm | 29.965001831054696 mm | 동일 | PASS |
| `y_neg` | -29.955002 mm | -29.955002441406233 mm | 동일 | PASS |
| `width` | 99.779974 mm | 99.77997436523435 mm | 동일 | PASS |
| `length` | 59.920004 mm | 59.92000427246093 mm | 동일 | PASS |
| `height` | 39.865005 mm | 39.86500537109375 mm | 동일 | PASS |
| `support_z` | 0.000000 mm | 0 | 0.0 | PASS |
| `dims_valid` | true | true | true | PASS |
| `support_z_valid` | true | true | true | PASS |
| `box_valid` | true | true | true | PASS |

Main PC 출력은 소수점 6자리 표시값이며 PostgreSQL/Spring은 원래 double precision 값을 유지하므로 표시 자릿수만 다르다.

## 5. 단위 변환 검증

Main PC result_store는 ROS 내부 길이 단위 `m`, PostgreSQL/Spring은 `mm`를 사용한다.

예:

```text
0.099779974... m × 1000
= 99.779974... mm
```

설정값 확인:

| Main PC | PostgreSQL / Spring | 판정 |
|---|---|---|
| `edge_drop_m = 0.0005` | `edge_drop_mm = 0.5` | PASS |
| `descend_speed_mps = 0.005` | `descend_speed_mmps = 5.0` | PASS |
| `slide_speed_mps = 0.01` | `slide_speed_mmps = 10.0` | PASS |
| `max_descend_m = 0.08` | `max_descend_mm = 80.0` | PASS |
| `max_slide_m = 0.08` | `max_slide_mm = 80.0` | PASS |
| `lift_height_m = 0.05` | `lift_height_mm = 50.0` | PASS |
| `drop_limit_m = 0.005` | `drop_limit_mm = 5.0` | PASS |

## 6. ScanConfig 저장·조회 검증

Main PC 실제 적용 설정이 PostgreSQL `scan_configs.config`에 저장되고 Spring Boot 상세 조회의 `scanConfig.config`에서 동일하게 조회됨을 확인하였다.

단위 변환이 없는 주요 값:

```text
contact_threshold_n = 3.0
debounce_n          = 3
over_force_n        = 30.0
motion_timeout_s    = 30.0
target_force_n      = 3.0
```

판정: **PASS**

## 7. 실패·미측정값과 정상 0 구분

실패 Scan:

```text
scan_id = 20260922-202652-9248
```

PostgreSQL 확인:

```text
z_top_mm        = NULL
z_top_valid     = false

x_pos_mm        = NULL
x_pos_valid     = false

x_neg_mm        = NULL
x_neg_valid     = false

y_pos_mm        = NULL
y_pos_valid     = false

y_neg_mm        = NULL
y_neg_valid     = false

width_mm        = NULL
length_mm       = NULL
height_mm       = NULL
dims_valid      = false

support_z_mm    = 0
support_z_valid = true

box_valid       = false
```

따라서:

```text
미측정  = NULL + valid=false
정상 0  = 0 + valid=true
```

로 구분됨을 확인하였다.

판정: **PASS**

## 8. FastAPI DB Commit 완료 구분

실행:

```bash
docker logs weld_made_fastapi 2>&1 \
  | grep -F "[DB] scan/result committed scan_id=20260922-205447-1618"
```

실제 출력:

```text
[DB] scan/result committed scan_id=20260922-205447-1618
```

MQTT 수신과 DB commit 완료를 구분하여 기록함을 확인하였다.

판정: **PASS**

## 9. 최종 판정

### TR-10: PASS

정상 Scan `20260922-205447-1618` 기준으로 결과 식별자, 측정값, 프레임, valid flag, 실제 적용 설정이 Main PC 원본 → PostgreSQL → Spring Boot 조회까지 일치하였다.

실패 Scan `20260922-202652-9248`에서는 미측정값이 `NULL + valid=false`, 정상값 `support_z=0`은 `0 + valid=true`로 보존되었다.

본 시험은 `source:=sim` 환경의 저장·조회 연동 검증이며 실기 치수 정확도 시험과는 구분한다.
